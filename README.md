# Playlist-Manager
Playlist Manager is a full-stack data structures project built using HTML, CSS, and JavaScript on the frontend and C++ (CGI) on the backend. It uses a singly linked list to manage songs and supports operations like add, delete, search, sort, and display, with dynamic updates handled through AJAX.
